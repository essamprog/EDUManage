@using EduManage.Application.DTOs.Courses
@model CourseDto

<div class="card h-100 border-0 shadow-sm hover-shadow" style="transition: transform .2s, box-shadow .2s;">
    <a asp-controller="Courses" asp-action="Details" asp-route-id="@Model.Id"
           class="text-decoration-none">
        <img src="@(Model.ThumbnailUrl ?? "/images/course-default.jpg")"
             class="card-img-top" alt="@Model.Title"
             style="height:160px;object-fit:cover" />
    </a>
    <div class="card-body d-flex flex-column">

        @if (Model.IsBestseller)
        {
            <span class="badge bg-warning text-dark mb-2" style="width:fit-content">
                <i class="bi bi-star-fill me-1"></i>Bestseller
            </span>
        }

        <h6 class="card-title fw-bold mb-1">
            <a asp-controller="Courses" asp-action="Details" asp-route-id="@Model.Id"
                   class="text-dark text-decoration-none stretched-link">
                @Model.Title
            </a>
        </h6>

        @* Instructor info *@
        <div class="d-flex align-items-center gap-2 mb-2">
            @if (!string.IsNullOrEmpty(Model.InstructorPicture))
            {
                <img src="@Model.InstructorPicture" alt="@Model.InstructorName"
                     style="width:24px;height:24px;border-radius:50%;object-fit:cover;flex-shrink:0;" />
            }
            else
            {
                var nameForInitials = Model.InstructorName ?? "?";
                var initials = string.Join("", nameForInitials
                    .Split(' ', System.StringSplitOptions.RemoveEmptyEntries)
                    .Take(2)
                    .Select(w => w[0]));
                <span style="width:24px;height:24px;border-radius:50%;background:linear-gradient(135deg,#4299e1,#7c3aed);
                             color:#fff;font-size:10px;font-weight:700;display:inline-flex;
                             align-items:center;justify-content:center;flex-shrink:0;">
                    @initials
                </span>
            }
            <span class="text-muted small text-truncate">@Model.InstructorName</span>
        </div>

        <div class="d-flex align-items-center gap-1 mb-2">
            <span class="fw-bold text-warning small">@Model.AverageRating.ToString("F1")</span>
            @for (int i = 1; i <= 5; i++)
            {
                <i class="bi bi-star@(i <= (int)Model.AverageRating ? "-fill" : i - 0.5m <= Model.AverageRating ? "-half" : "") text-warning"
                   style="font-size:11px"></i>
            }
            <span class="text-muted" style="font-size:11px">(@Model.TotalStudents)</span>
        </div>

        <div class="d-flex align-items-center gap-2 small text-muted mb-3">
            <span><i class="bi bi-play-circle me-1"></i>@Model.TotalLessons Lessons</span>
            <span><i class="bi bi-bar-chart me-1"></i>@Model.Level</span>
        </div>

        <div class="mt-auto">
            <span class="fw-bold text-primary fs-5">EGP @Model.Price.ToString("N0")</span>
        </div>

    </div>
</div>